name: Deploy Documentation

on:
  push:
    branches:
      - main
      - dev
      - chore
      - doc
  pull_request:
    types: [opened, synchronize, reopened]
    branches:
      - main
      - dev
      - chore

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.11'

      - name: Install poetry
        run: |
          python3 -m pip install --upgrade pip
          curl -sSL https://install.python-poetry.org | python3 -
          export PATH="$HOME/.local/bin:$PATH"
          poetry --version

      - name: Install project dependencies
        run: |
          poetry install --no-root

      - name: Install Sphinx and dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y python3-sphinx
          pip install sphinx sphinx-rtd-theme sphinx-intl

      - name: Generate API documentation
        run: |
          poetry run sphinx-apidoc -o docs/source/ ./zae_engine

      - name: Extract translations
        run: |
          cd docs
          poetry run sphinx-build -b gettext source locale

      - name: Update translations
        run: |
          cd docs
          poetry run sphinx-intl update -p locale -l ko

      - name: Build English documentation
        env:
          GA_MEASUREMENT_ID: ${{ secrets.GA_MEASUREMENT_ID }}
        run: |
          cd docs
          poetry run make clean
          poetry run make -e SPHINXOPTS="-D language=en" html
          mkdir -p ../final_build/en
          cp -r build/html/* ../final_build/en/

      - name: Build Korean documentation
        env:
          GA_MEASUREMENT_ID: ${{ secrets.GA_MEASUREMENT_ID }}
        run: |
          cd docs
          poetry run make clean
          poetry run make -e SPHINXOPTS="-D language=ko" html
          mkdir -p ../final_build/ko
          cp -r build/html/* ../final_build/ko/

      - name: Create auto redirect index.html
        run: |
          mkdir -p docs/final_build
          echo '<!DOCTYPE html><html><head><title>Redirecting...</title><script>const userLang = navigator.language || navigator.userLanguage;if (userLang.startsWith("ko")) {window.location.href = "./ko/";} else {window.location.href = "./en/";}</script><noscript><p>Your browser does not support JavaScript. Please select your language:</p><ul><li><a href="./en/">English</a></li><li><a href="./ko/">한국어</a></li></ul></noscript></head><body><p>Redirecting based on your browser language...</p></body></html>' > docs/final_build/index.html

      - name: Deploy to GitHub Pages
        uses: peaceiris/actions-gh-pages@v3
        with:
          github_token: ${{ secrets.PAT_GITHUB }}
          publish_branch: docs
          publish_dir: docs/final_build
          force_orphan: true
